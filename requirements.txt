# PMRS - Auto-generated requirements
# Core ML dependencies
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
transformers>=4.30.0
timm>=0.9.0

# Web framework
fastapi>=0.100.0
starlette>=0.27.0
uvicorn>=0.23.0

# Database & ORM
sqlalchemy>=2.0.0

# LLM & AI
langchain>=0.0.200

# Data & Validation
pydantic>=2.0.0
pyyaml>=6.0

# Standard library (no install needed): typing, logging, datetime, os, pathlib, json, time, sys, struct, socket, enum, contextlib, asyncio
